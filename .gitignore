node_modules/
dist/
*.log
.env
.DS_Store
# dados de candidatos (podem conter PII) e saidas geradas — nunca versionar
data/
candidatos/
saida/
# sessao logada do navegador (persistente) — nunca versionar
.linkedin-session/
.browser-session/
